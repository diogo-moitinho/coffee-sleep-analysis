# Dependências do app Streamlit (streamlit_app.py).
#
# O app só apresenta números já medidos: não treina, não lê o dataset e não
# carrega modelo. Por isso não precisa de scikit-learn, LightGBM, scipy,
# statsmodels nem seaborn — o que torna o build no Streamlit Cloud rápido e
# difícil de quebrar.
#
# O ambiente dos notebooks está em requirements-notebooks.txt.

streamlit>=1.40
pandas>=2.0
plotly>=5.20
